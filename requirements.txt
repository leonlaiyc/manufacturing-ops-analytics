# Pinned loosely; exact versions locked after M2 runs end to end.
pandas>=2.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
plotly>=5.9
networkx>=2.8
scikit-learn>=1.3
shap>=0.44
mcp>=1.20            # MCP stdio server wrapper for the agent tool layer (src/agent/mcp_server.py)
